// this should be stored in database, but now I did not wanted to create so many new files just to store a token
// I am using array because I don't expect for a huge amount of data that should be stored

let tokenContainer = [
  {token: 'valami', email: 'valami'}
]

module.exports = tokenContainer