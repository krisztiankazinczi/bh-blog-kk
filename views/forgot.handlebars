<div class="col-12">
  <div class="d-flex justify-content-center align-items-center vh-100">
    <div class="container">
      <div class="row">
        <div class="col-3"></div>
        <div class="col-6">

          {{#unless pwChangeSuccess}}
          {{#unless serverError}}
          {{#unless success}}
          <h2>Forgot your password?</h2>
          <p class="mt-5">
            Enter your email address below to reset your password. You will be sent an email which you will need to be
            open
            to continue. You may need to check your spam folder.
          </p>
          <form action="/forgot" method="POST" class="mt-4">
            <div class="form-group">
              <div class="row mt-3">
                <div class="col-5 text-left">
                  <label for="email">Email: </label>
                </div>
                <div class="col-7">
                  <input type="text" class="form-control" id="email" name="email">
                  <div class="text-right">
                    <input type="submit" class="btn btn-info text-white form-control mt-3" value="Submit">
                  </div>
                </div>
                {{#if userError}}
                <p class="mt-5 text-danger">
                  This is email is not registered in our system. Please try to add your email address that is registered
                  on this page.
                </p>
                {{/if}}
              </div>
          </form>
          {{/unless}}
          {{/unless}}
          {{/unless}}

          {{#if serverError}}
          <h2>Error</h2>
          <p class="mt-5">
            We are really sorry, but something went wrong and we were not able to send the email to the provided email
            address.
          </p>
          {{/if}}

          {{#if success}}
          <h3>Your password reset email has been sent!</h3>
          <p class="mt-5">
            We have sent a password reset email to your email address: {{emailTo}}<br>
            Please check your inbox to continue!
          </p>
          {{/if}}

          {{#if pwChangeSuccess}}
          <h3>Your password have been changed successfully</h3>
          <p class="mt-5">
            Please try to log in the webpage with your new password.<br>
            If it's still not working, please contact us here: <a href="#">support@blogpost.com</a>
          </p>
          {{/if}}


        </div>
        <div class="col-3"></div>
      </div>
    </div>
  </div>
</div>